<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace = "popular.PopularBean">
	<select id="getAllPopular" resultType="PopularBean">
		select * from popular
		order by popular_count
	</select>
</mapper>